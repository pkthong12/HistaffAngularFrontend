import { MapAvatarToServerPipe } from './map-avatar-to-server.pipe';

describe('MapAvatarToServerPipe', () => {
  it('create an instance', () => {
    const pipe = new MapAvatarToServerPipe();
    expect(pipe).toBeTruthy();
  });
});
