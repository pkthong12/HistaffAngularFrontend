import { TableCellPipe } from './table-cell.pipe';

describe('TableCellPipe', () => {
  it('create an instance', () => {
    const pipe = new TableCellPipe();
    expect(pipe).toBeTruthy();
  });
});
